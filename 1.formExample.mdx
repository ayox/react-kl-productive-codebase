import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github,nightOwl, vsDark } from "@code-surfer/themes";

import forms from './form1.png'



<CodeSurferColumns themes={[vsDark, github]}>

<Step>

```jsx  title="Simple Form"

import React from "react";
import { Form, Input, Button } from "antd";

function EmployeeForm(props) {
  const { getFieldDecorator, validateFields } = props.form;
  function handleSubmit(e) {
    e.preventDefault();
    validateFields((err, values) => {
      // submit logic
      console.log(values);
    });
  }
  return (
    <Form layout="horizontal" onSubmit={handleSubmit}>
      <Form.Item label="Full Name">
        {getFieldDecorator("fullName")(
          <Input placeholder="Enter your full name" />
        )}
      </Form.Item>
      <Form.Item label="Passport">
        {getFieldDecorator("passport")(
          <Input placeholder="Please enter your passport no" />
        )}
      </Form.Item>
      <Form.Item>
        <Button type="primary" htmlType="submit">
          Submit
        </Button>
      </Form.Item>
    </Form>
  );
}

const WrappedEmployeeForm = Form.create({ name: "employee_form" })(
  EmployeeForm
);

export default WrappedEmployeeForm;


```

<Image src={forms} size='80%'></Image>


</Step>

<Step>

```jsx 14:34 subtitle="That's what you will read when you maintain"


import React from "react";
import { Form, Input, Button } from "antd";

function EmployeeForm(props) {
  const { getFieldDecorator, validateFields } = props.form;
  function handleSubmit(e) {
    e.preventDefault();
    validateFields((err, values) => {
      // submit logic
      console.log(values);
    });
  }
  return (
    <Form layout="horizontal" onSubmit={handleSubmit}>
      <Form.Item label="Full Name">
        {getFieldDecorator("fullName")(
          <Input placeholder="Enter your full name" />
        )}
      </Form.Item>
      <Form.Item label="Passport">
        {getFieldDecorator("passport")(
          <Input placeholder="Please enter your passport no" />
        )}
      </Form.Item>
      <Form.Item>
        <Button type="primary" htmlType="submit">
          Submit
        </Button>
      </Form.Item>
    </Form>
  );
}

const WrappedEmployeeForm = Form.create({ name: "employee_form" })(
  EmployeeForm
);

export default WrappedEmployeeForm;

```

<Image src={forms} size='80%'></Image>


</Step>


</CodeSurferColumns>
